# ui/ — presentation config (UI as Config)

Themes, layout slots, copy keys. Consumed by `ui_shell` only.

```text
ui/
  brand/logo.png   # mark (tray/README); no text lockup yet
  default.toml
  tokens.css
  preview/
```

Business plugins must not read these files.
