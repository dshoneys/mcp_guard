# REQ-TRAY-UI — UI acceptance

- [x] Preview opens via `python scripts/ui_preview.py` → `/REQ-TRAY-UI/`
- [x] Tokens aligned between `tokens.css` and `default.toml` (mapping.md)
- [x] Actions and state ids match UX + `TrayActionId` / `build_menu`
- [x] Lead sets `ui_status: accepted` after review

## Lead decision

**Accepted** (2026-08-10). Native tray must reuse the same action ids and copy keys; pixel parity with OS chrome is out of scope.
