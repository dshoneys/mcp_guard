# REQ-VAULT-UI — ia

**Secondary view** inside the main window (not on the home hero).

- Entry: home → “密钥保险箱” / “Secret vault”
- Exit: back to home
- List: name + updated
- Form: name + secret input + Save
- Row action: Delete
- Hint: NoContext MCP (refs only)
